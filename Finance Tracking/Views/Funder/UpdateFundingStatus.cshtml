@model Finance_Tracking.Models.Bursar_Fund

@{
    ViewBag.Title = "UpdateFundingStatus";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Update Funding Status</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Application_ID)
        <div class="form-group">
            <p>Current status for application @Html.DisplayFor(model => model.Application_ID) is @Html.DisplayFor(model => model.Funding_Status)</p>
        </div>
        <div class="form-group">
            @Html.LabelFor(model => model.Funding_Status, htmlAttributes: new { @class = "control-label col-md-3" })
            <div class="col-md-9">
                @Html.DropDownListFor(model => model.Funding_Status, new List<SelectListItem>
                     {
                        new SelectListItem{ Text="Select", Value = "Select" , Selected =true},
                        new SelectListItem{ Text="Funded", Value = "Funded" },
                        new SelectListItem{ Text="Rejected", Value = "Rejectd" },
                        }, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Funding_Status, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="container-btn">
                <div class="left-btn-flex col-md-6">
                    <a href='@Url.Action("ViewBursar", "Funder", new { id = Model.Application_ID })'>
                        <input type="button" name="selection" value="Cancel" class="btn btn-default" />
                    </a>
                </div>
                <div class="right-btn-flex col-md-6">
                    <input type="submit" name="selection" value="Update" class="btn btn-default" />
                </div>
            </div>
        </div>
    </div>
}

