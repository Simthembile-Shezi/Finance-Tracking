@model Finance_Tracking.Models.Application

@{
    ViewBag.Title = "ShowApprovedApplications";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Approved Applications</h2>
<span class="text-danger">@ViewBag.NoApprovedApplications</span>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Bursar_Fund.Application_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bursar_Fund.Funding_Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bursar_Fund.Approved_Funds)
        </th>
    </tr>

    @foreach (var item in Model.Bursar_Funds)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Application_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Funding_Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Approved_Funds)
            </td>
            @*<td>
                @Html.ActionLink("View Fund Request", "ViewFundRequest", new { id = item.Application_ID })
            </td>*@
        </tr>
    }
</table>
<div class="container-btn">
    <div class="left-btn-flex">
        <a href='@Url.Action("Index", "Student")'>
            <div>
                <input type="button" name="selection" value="Back" class="btn btn-default" />
            </div>
        </a>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
